Currently available API - openai, gemini, googlesearch

Request us directly for more API Key support for now 

In the future, we will create an option to register your own API Key

In this repo, we provide a sample script for you to provide us.

Once you are done adapting the template for your custom tool,
You dont have to email us, simple create a pull request.

Instruction

Clone your repo and copy it into the project root

ORG=[your username or github organization name]
REPO=[your repo name on github]
git clone git@github.com:$ORG/$REPO.git || true
cp -r $REPO ./$REPO || true

For example,

git clone git@github.com: